se 2 <=  n <= 3, resolver por forca bruta
se n>3 dividir o conjunto de pontos em 2 subconjuntos Pe e Pd
    conquistar:recursivamente determinar os pontos minimais de cada subconjuntos
    combinar: e ver quais sao de fato os pontos minimais

pontos_minimais(ini, fim){
    n = fim - ini + 1
    if (n == 2) #se tiver so 2 pontos resolve por forca bruta
        if(x[0] < x[1] e y[0] < y[1]) return x[0]
        if else(x[0] < x[1] e y[0] < y[1]) return x[1]
        else return 0
    else
        

    
}